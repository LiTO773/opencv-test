/**
 * The prototype intentionally consumes the workbench's one hardcoded schema.
 * Production schema delivery remains outside this sprint.
 */
export { schema as hardcodedBubbleGradingSchema } from '../../../tools/schema-preview/schema';
